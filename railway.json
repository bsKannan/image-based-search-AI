{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "nixpacks",
    "context": "backend"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyMaxRetries": 5
  }
}
